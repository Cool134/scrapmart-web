--- src/app/dashboard/seller/upload/page.tsx
+++ src/app/dashboard/seller/upload/page.tsx
@@ -4,6 +4,7 @@
 import { useRouter } from 'next/navigation';
 import { createListing } from '@/lib/firestore';
 import { Camera, Loader2, UploadCloud, ChevronLeft, Sparkles, CheckCircle2, TrendingUp } from 'lucide-react';
+import { getMarketRate } from '@/lib/marketRates';
 
 export default function SellerUploadPage() {
   const router = useRouter();
@@ -11,7 +12,6 @@
   const [formData, setFormData] = useState({
     material: '',
     thickness_mm: '',
-    price: '',
     location: '',
     description: '',
     imageURL: '',
@@ -34,6 +34,7 @@
     
     // Trigger AI analysis
     setAnalyzing(true);
     setAnalysisResult(null);
 
@@ -74,10 +74,12 @@
     e.preventDefault();
     setLoading(true);
+    
+    const livePrice = getMarketRate(formData.material);
 
     try {
       await createListing({
         title: `₹{formData.material} - ₹{formData.thickness_mm || 0}mm`,
         material: formData.material,
         thickness_mm: Number(formData.thickness_mm || 0),
-        price: Number(formData.price || 0),
+        price: livePrice,
         location: formData.location,
         description: formData.description,
         imageURL: formData.imageURL || '/scrap/scrap-1.svg',
@@ -88,7 +88,7 @@
         estimated_weight_kg: 10,
         surface_area_cm2: 10000,
         confidence: analysisResult?.confidence || 0.95,
-        suggested_price_inr: Number(formData.price || 0),
+        suggested_price_inr: livePrice,
         outline_points: [],
         surface_condition: 'Good'
       });
@@ -103,7 +103,11 @@
   };
 
   const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement>) => {
     setFormData({ ...formData, [e.target.name]: e.target.value });
   };
+
+  const currentLivePrice = getMarketRate(formData.material || analysisResult?.prediction || analysisResult?.material || '');
 
   return (
     <div className="min-h-screen bg-[#F4F7FC] pb-24 text-gray-900 font-sans">
@@ -233,21 +237,13 @@
                   Thickness (mm)
                 </label>
               </div>
-              <div className="relative">
-                <input 
-                  required 
-                  type="number" 
-                  step="0.01" 
-                  name="price" 
-                  value={formData.price} 
-                  onChange={handleChange} 
-                  placeholder=" "
-                  className="peer w-full bg-gray-50 text-gray-900 border-none rounded-2xl px-4 pt-6 pb-2 focus:ring-2 focus:ring-blue-500 focus:bg-blue-50/50 outline-none transition-all" 
-                />
-                <label className="absolute left-4 top-4 text-sm font-medium text-gray-500 peer-placeholder-shown:text-base peer-placeholder-shown:top-4 peer-focus:top-2 peer-focus:text-xs peer-focus:text-blue-600 transition-all cursor-text pointer-events-none">
-                  Price per Kg (₹)
-                </label>
+              <div className="relative bg-blue-50/50 border border-blue-100 rounded-2xl px-4 py-3 flex flex-col justify-center">
+                 <span className="text-[10px] font-bold text-blue-600 uppercase tracking-widest flex items-center mb-1">
+                   <TrendingUp className="w-3 h-3 mr-1" />
+                   Live Market Price
+                 </span>
+                 <span className="font-black text-xl text-gray-900">₹{currentLivePrice > 0 ? currentLivePrice : '---'} <span className="text-sm font-medium text-gray-500">/ kg</span></span>
               </div>
             </div>
 
